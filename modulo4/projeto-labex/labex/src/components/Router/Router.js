import React from 'react';
import {BrowserRouter} from `react-router-dom`

function Router() {
  return (
    <BrowserRouter>
    <Routes>
        <Router/>
    </Routes>
    </BrowserRouter>
  );
}

export default Router;